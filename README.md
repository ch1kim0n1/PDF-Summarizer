### libraries to install
1. pip install --user tf-keras
2. pip install --user transformers

# BookWormAI

Resperatory for the AI that can read books and answer questions accordingly.

AI that recognize a book and then answers any follow up questions about it. You need to pick a book from a library/dropdown of books that are available. If not available, then make a request to make it.

# Ideas
* use OpenAI/Claude to grab pdf files
* process pdf files and give a responce to a prompt of a user
* create website wrapper around it (React)
* use qith books, so AI must be able to cover 100s of pages
